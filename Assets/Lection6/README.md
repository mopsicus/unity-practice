# Задание

1. Есть класс `PlayerData`, который хранит информацию о здоровье игрока.
Нужно написать юнит-тест, проверяющий, что при создании объекта здоровье (health) и имя (name) инициализируются корректно. (EditMode тест)

2. Нужно протестировать, что созданный на сцене куб действительно перемещается на заданное расстояние. (PlayMode тест)

Запустить через Test Runner, убедиться что все тесты зелёные (Passed)

## Задание для дебага

1. Запустите проект в Debug режиме (через IDE)

2.	Поставьте breakpoint:
 - в методе `Start()` на строке с `CalculateHealth()`;
 - в методе `CalculateHealth()` на строке `return damage`;
 - 
3.	Используя пошаговое выполнение (Step Into / Step Over), проследи, какие значения получает переменная `current` и что возвращается.

4.	Исправь ошибки, чтобы вывод в консоль показывал:

```
Здоровье после удара: 70
Игрок жив: 70
```